[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm",
  "pnpm install"
]

[phases.build]
cmds = [
  "node artifacts/api-server/build.mjs"
]

[start]
cmd = "node artifacts/api-server/dist/index.mjs"
