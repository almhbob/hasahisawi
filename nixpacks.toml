[phases.setup]
  nixPkgs = ["nodejs_22"]

  [phases.install]
  cmds = [
    "npm install -g pnpm",
    "pnpm install"
  ]

  [phases.build]
  cmds = [
    "node artifacts/api-server/build.mjs"
  ]

  [start]
  cmd = "node artifacts/api-server/dist/index.mjs"
  