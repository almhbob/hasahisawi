name: "[DISABLED] Extract Keystore"
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow was used once to extract the keystore. Now disabled — keystore configured via EAS credentials."
