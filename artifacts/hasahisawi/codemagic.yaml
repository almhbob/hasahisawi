workflows:
  android-release:
    name: Android Release AAB (EAS)
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      node: 22.x
      groups:
        - hasahisawi_firebase
        - hasahisawi_eas
      vars:
        PACKAGE_NAME: com.almhbob.hasahisawi

    cache:
      cache_paths:
        - $HOME/.npm

    scripts:
      - name: Install pnpm
        script: npm install -g pnpm

      - name: Install EAS CLI
        script: npm install -g eas-cli

      - name: Install dependencies
        script: pnpm install --frozen-lockfile

      - name: Authenticate with Expo
        script: |
          echo $EXPO_TOKEN | eas whoami || eas login --token $EXPO_TOKEN

      - name: Build Android AAB via EAS
        working_directory: artifacts/hasahisawi
        script: |
          eas build \
            --platform android \
            --profile production \
            --non-interactive \
            --no-wait

    publishing:
      email:
        recipients:
          - almhbob2026@gmail.com
        notify:
          success: true
          failure: true
